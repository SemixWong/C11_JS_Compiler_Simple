// 测试1: 基本变量声明
let x = 10;
const y = 20;
var z = 30;

// 测试2: 函数声明
function add(a, b) {
    return a + b;
}

// 测试3: 普通函数表达式
const multiply = function(a, b) {
    return a * b;
};

// 测试4: 对象字面量
const person = {
    name: "张三",
    age: 25,
    greet: function() {
        console.log("Hello!");
    }
};

// 测试5: 数组字面量
const numbers = [1, 2, 3, 4, 5];
